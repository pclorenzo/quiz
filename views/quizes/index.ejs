<form method="GET" action="/quizes?search">
	<input type="text" name="search" placeholder="texto búsqueda"></input>
	<input class ="normal_btn" type="submit" value="Buscar"></input>
</form>
<table>
<% var i; for(i=0; i<quizes.length; i++) { %>
	<tr>
		<td class="just_izq"><a href="quizes/<%= quizes[i].id %>"><%= quizes[i].pregunta %></a></td>
		<td class="just_izq_pad_10px"> <%= quizes[i].tema %></td>
		<% if (session.user) { %>
		<td class="just_izq_pad_10px"> <a href="quizes/<%= quizes[i].id %>/edit"><button>editar</button></a></td>
		<td class="just_izq_pad_10px"> 
			<form method="POST" action="quizes/<%= quizes[i].id %>?_method=delete">
				<button type="submit" onClick="return confirm('Borrar: <%= quizes[i].pregunta %>')">borrar</button>
			</form>
		</td>
		<% } %>
	</tr>
<% } %>
</table>
<% if (session.user) { %>
<a href="/quizes/new"><button type="button">Crear pregunta</button></a>
<% } %>